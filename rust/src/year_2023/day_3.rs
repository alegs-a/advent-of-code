#![allow(warnings)]

use std::collections::HashMap;

struct Position {
    x: usize,
    y: usize,
    value: i32,
}

pub fn part_1(input: String) -> String {
    let nums: Vec<Position> = Vec::new();
    for (i, line) in input.lines().enumerate() {
        for (j, char) in line.char_indices() {

        }
    }
    String::new()
}

pub fn part_2(input: String) -> String {
    String::new()
}
